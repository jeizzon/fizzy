<% if Current.user %>
  <style>
    @scope ([data-creator-id="<%= Current.user.id %>"]) to ([data-creator-id]:not([data-creator-id="<%= Current.user.id %>"])) {
      [data-only-visible-to-others] { display: none; }
    }
    @scope ([data-creator-id]:not([data-creator-id="<%= Current.user.id %>"])) to ([data-creator-id="<%= Current.user.id %>"]) {
      [data-only-visible-to-you] { display: none; }
    }
  </style>
<% end %>
